# Copy to .env or .env.local (both gitignored). Never commit real keys.
# SiteSmith creative LLM pass uses the first present key.

XAI_API_KEY=
# GROK_API_KEY=
# SITESMITH_CREATIVE_MODEL=grok-4-fast-reasoning
