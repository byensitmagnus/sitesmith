# Direction record

Surfaces: shop
Subject: a two-person vegetable tannery that runs six pits on a ninety day cycle
Palette: `#0f0f12` the pit at night, `#c8ff2f` the volt line, `#e8e8ea` chalk
Type: display "Courier New" for the pit labels, body "Georgia" for the ledger
Signature: the wet line that runs under every heading (`.wetline`)

One-offs:
- `0.5px` the wet line is a hairline and a hairline is not a scale step

Deliberate:
- `gradient-text` the drum label bleeds like liquor into leather, and the bleed is the mark
