# Direction record

Surfaces: shop
Subject: a two-person vegetable tannery that runs six pits on a ninety day cycle
Palette: `#e6ecf2` the paper the ledger is kept on, `#dccfb8` the sizing drum, `#b9a487` bark before it is ground
Type: display "Courier New" for the pit labels, body "Georgia" for the ledger
Signature: the wet line that runs under every heading (`.wetline`)

One-offs:
- `0.5px` the wet line is a hairline and a hairline is not a scale step

Deliberate:
- `gradient-text` the drum label bleeds like liquor into leather, and the bleed is the mark

## Answer to the risk
The risk above is answered by `.wetline`, the signature that runs under every heading and keeps the page reading as one object rather than as a document with sections.

## The shell
The loft name is set as plain text and nothing else. There is no header link, no navigation and no footer, and no action a reader can take.

