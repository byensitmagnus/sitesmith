---
title: LICENSE — ponytail
ai_generated: "(C)"
---

# Licence

MIT (`LICENSE`, full text present, copyright DietrichGebert 2026). Confirmed on disk at
`/c/Users/Usmo1/AppData/Local/Temp/.../upstream/ponytail/LICENSE:1-22`. Per the source list for
this autopsy, MIT text from this repo may be redistributed with notice.

# What was and wasn't reproduced

This autopsy describes every mechanism in its own words. No verbatim `SKILL.md` prose, rubric text,
or benchmark copy exceeding the task's one-quote/under-15-words allowance was reproduced; short
identifying fragments (e.g. the `ponytail:` comment tag itself, file/function names, the exact
phrase "judge not trustworthy; refusing to judge the matrix" as a single quoted example of the
refusal message) are cited as file:line pointers rather than reproduced as flowing prose, consistent
with MIT's requirement to retain attribution rather than a bar on quotation.

# Attribution requirement if any text is ported into SiteSmith

MIT requires the license and copyright notice be included "in all copies or substantial portions of
the Software." If SiteSmith's rebuild ports any of ponytail's actual prose (rather than the
mechanism idea, reworded) — e.g. the seven-rung ladder's exact wording — the MIT notice must travel
with it. The `adopt`/`adapt` mechanisms in `MECHANISMS.json` are written as descriptions of the
underlying idea for SiteSmith to re-implement in its own words, which does not itself trigger the
attribution requirement; only literal reuse of ponytail's text would.

# No other licences encountered

No third-party dependency licences to reconcile: root `package.json` declares 0 runtime
dependencies (`grep -c "dependencies" package.json` → 0). No `NOTICE` file is required or present,
consistent with MIT (Apache-2.0 sources require a NOTICE; this is not one).
