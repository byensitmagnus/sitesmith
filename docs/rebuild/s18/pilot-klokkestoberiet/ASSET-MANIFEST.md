# Asset manifest, Klokkestøberiet Hjelm

Nothing was supplied by the client. No photograph of this foundry exists in the brief, so
none appears on the page and none was generated. Everything below is drawn in this file,
which makes the provenance this repository and the licence its licence.

| id | what | where | source | licence | state |
| --- | --- | --- | --- | --- | --- |
| a1 | section through a bell: crown, shoulder, waist, flare and sound bow, with the back wall visible through the cut | `index.html`, `.klokke` | drawn here as inline SVG | original, MIT with this repository | ready |
| a2 | the heat field, one light source behind the bell mouth, rising with scroll | `index.html`, `.aabning::before` | drawn here as a CSS radial gradient | original, MIT | ready |
| a3 | sand and soot grain over the whole ground | `index.html`, `body::before` | drawn here as an inline SVG turbulence filter in a data URI | original, MIT | ready |
| a4 | the overtone rings, seven struck from the sound bow | `index.html`, `.ringe` | drawn here as inline SVG | original, MIT | ready |

What each one carries, from `LOOK.md` section 3: without a1 the page is a schedule with a
heading; without a2 it is a drawing on a dark rectangle; without a3 it is flat; without a4
the seven ratios are a table nobody reads.

a1 and a4 are schematic. The visible source note at the foot of the page says so, because
neither is a measured drawing of a particular bell and a reader could otherwise take them
for one.
