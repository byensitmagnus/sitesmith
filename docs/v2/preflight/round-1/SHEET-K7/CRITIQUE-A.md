---
reviewer: A
reviewer-id: reviewer-a-blind
run-id: preflight-e7a20b478917
label: SHEET-K7
locked: 2026-07-28T12:12:00Z
sha256: 9b1377a96489941b4d431eac2834355449c1aecc9e780931e240c8566097577e
brief-sha256: e06aafaf3f4a68092d5ab23aa3fb0c9513174cdabd803b865ef8c1005fad9d1e
rubric-sha256: d9932ed6dc486719d74e6884db7a087bc9563bea07006233790845e2d49cf4f8
sheet-sha256: 4db6a5e239e67f2a2885393c99e81d65b899a9691c758beb924438bac854cbc6
---
primary-criticism: At 390px every row prints its own data on top of its own illustration — "Gyle 214" is struck through by the barrel's top hoop, the cask count sits inside the barrel and the due date lies across the lower hoop — so the mobile cask list, which is the whole product, cannot be read.
direction: 6
specificity: 5
type: 5
colour: 5
assets: 4
hierarchy: 4
production-readiness: 2

notes:
- direction — desktop rows: the largest and brightest object in every row is the white cask outline, which encodes size, while the state a cellarman came to scan for is a pill at the far right; the visual argument is about the one variable nobody is looking for, and it sets a 157px row height to say it.
- specificity — take the logo and the copy off and this is the category default rendered competently: navy ground, three filter chips with the active one filled amber, mono column headers in letterspaced caps, a coloured status pill and a ghost button on every row, an empty secondary table below. Only the cask drawing is specific, and it earns its place — the kilderkin is visibly wider than the firkin, so the drawing is doing real work — but it is the single element that could not be lifted into any other admin tool.
- type — the pairing is a stock UI sans plus a stock mono with nothing in the setting that would be recognisable on a second page, and on desktop the DUE BACK column mixes the two: "Today" is set in the sans while "3 d late" and "+4 d" directly below it are mono — one column, two faces.
- colour — the same amber does two unrelated jobs: it fills the selected filter chip "Everything" in the toolbar and the "ON TRADE" status pill in row three, so "you chose this" and "this cask's state" read as the same signal from across the room.
- assets — mobile: the cask illustration collides with the text of its own row at every size and the "DUE TODAY" pill begins at a mid-row indent and runs past the right edge while "Book in" hangs alone below at the container margin; desktop below the fold: the "Booked in this week" rules stop at about three quarters of the container width while the main table above and the footer rule both run full width.
- hierarchy — desktop STATE column: "DUE TODAY" is near-white on near-black and is the highest-contrast object in the row, while "OVERDUE" is a soft coral with visibly less contrast, so the eye lands on the two routine rows before the late one; the pills also outweigh the real control beside them, "Book in", which is the dimmest element in the row and reads as a label while the labels read as buttons.
- production-readiness — mobile: overlapping, illegible text in every row of the primary table is a blocker, not a polish item; none of this view could go in front of a cellarman tomorrow.
